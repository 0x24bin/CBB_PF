201中发送方代码从SendID改成SenderId  (已修改)

201中业务类型的四个值都保留不变，只是1和3两种情况不需要往海关发送备案数据。但商品数据还是要录入我们的系统，否则在301订单信息中无法选择商品。（已修改）

201中电商平台代码和电商平台名称从必填改为可选（EBP_CODE已修改）

201中代理企业代码和代理企业名称改为必填（AGENT_CODE已修改）

201中商品编码改为必填（G_CODE已修改）

201中备案价格格式改为NUMERIC(18,2)（PRICE已修改）

201中删除物品税号和行邮税率两个字段，在页面上不显示，发送报文中删除，数据库表结构中删除（TAX_CODE，TAX_RATE已修改）

 

301中发送方代码从SendID改成SenderId（已修改）

301中商品货款格式改为NUMERIC(18,2)（GOODS_VALUE已修改）

301中行邮税费的中文名称改为综合税费，在界面上；格式改为NUMERIC(18,2)（TAX_FEE已修改）

301中运费格式改为NUMERIC(18,2)（FREIGHT已修改）

301中商品备案编号改为可选，进口时不填，出口时填（G_NO已修改）

301中删除物品税号和行邮税率两个字段，在页面上不显示，发送报文中删除，数据库表结构中删除（TAX_CODE，TAX_RATE已修改）

301中数量，单价和总价格式改为NUMERIC(18,2)（QTY，PRICE，TOTAL已修改）

 

501中发送方代码从SendID改成SenderId（已修改）

501中运输费用，报价费用，毛重，净重格式修改（FREIGHT，INSURE_FEE，WEIGHT，NET_WEIGHT已修改）

 

503中发送方代码从SendID改成SenderId（已修改）

 

现有的601清单被拆分为4种不同业务类型下的清单。601变为一般进口清单，604是保税进口清单，607是一般出口清单，610是保税出口清单，701是备货模式清单。先改601，再新增607。6其他暂时不做。601和607数据库字段都是一样的，只是发送的报文中几个固定字段不同。
(依据订单中的order_type修改报文，已修改)

601中发送方代码从SendID改成SenderId（已修改）

601中业务类型字段默认填1，可以将其从界面上删除（有问题，无此字段）

601中运输工具，航次号和分运单号改为必填（TRAF_NAME，VOYAGE_NO，BILL_NO已修改）

601中增加总运单号（MAIN_BILL_NO已修改）

601中行邮税费的中文名称改为综合税费，在界面上；格式改为NUMERIC(18,2)（TAX_FEE已修改）

601中商品备案编号在界面上删除，发送报文中删除，数据库中保留（G_NO已修改）

601中商品编码改为必填（G_CODE已修改）

601中法定数量和法定计量单位改为必填（QTY1，UNIT1已修改）

601中几个数量的格式修改（QTY，QTY2，ONE_PRICE，TOTAL已修改）
